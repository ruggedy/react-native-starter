import color from 'color';

import { Platform } from 'react-native';


const colors = {
    primary: '#ED1727',
    primaryColorText: '#fff'
}

export default {
    
    // Badge
    badgeBg: colors.primary,
    badgeColor: colors.primaryColorText
}